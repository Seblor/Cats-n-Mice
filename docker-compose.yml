name: cats-n-mice

services:
  bot:
    container_name: cats-n-mice-bot
    image: oven/bun:1-alpine
    working_dir: /app
    command: ["sh", "docker-entrypoint.sh"]
    restart: unless-stopped
    user: "1000:1000"

    # Load secrets from bot/.env (see bot/.env.example)
    env_file:
      - .env

    volumes:
      # Mount source directories — no image build needed
      - ./:/app
